# Compiled binaries (root-level only — /cmd/terrakit/ is source, not a binary)
/terrakit

# Ephemeral plan artefacts generated by `make plan`
.terrakit.tfplan
.terrakit.plan.json

# Terraform working files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json

# OS / editor noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/
